<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">
        </a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="products">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Browse Products
            </NavLink>
        </div>
        
        <!-- Only used for inserting data into database -->
        <div class="nav-item px-3">
            <div class="nav-item px-3">
                <button class="btn btn-primary" @onclick="RunDatabase">Run Database</button>
            </div>
        </div>
    </nav>
</div>


@code
{
    // Only used for inserting data into database
    public void RunDatabase()
    {
        CreateProducts createProducts = new CreateProducts();

        List<Headset> allProducts = createProducts.CreateListOfProducts();

        createProducts.InsertDummyDataInDB(allProducts);
    }
}
